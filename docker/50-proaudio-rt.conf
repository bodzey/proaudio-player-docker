# ProAudio Player runs without desktop Portal or RTKit services.
# Preserve direct RLIMIT_RTPRIO scheduling when the host grants it.
module.rt.args = {
  rlimits.enabled = true
  rtportal.enabled = false
  rtkit.enabled = false
}
